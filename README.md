# wwdc c dialect thing idk

transpile to c99
check with tinycc

pointer types (nullable ~, non nullable *, owned ^)
array slices

kill goto
kill lossy implicit conversion
kill pointer arithmetic, type punning

downgrade preprocessor (make #define like c#)
fix switch (auto add break)

lightweight generic(?)
better mem management

idk more
